name: jovi_app
description: "A new Flutter project for Jovi AR World."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0 # Asegúrate de que esta línea esté presente
  lucide_icons: ^0.257.0 # Asegúrate de que esta línea esté presente
  
  # Paquetes originales:
  mapbox_maps_flutter: ^2.12.0
  cached_network_image: ^3.4.0
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  
  # Firebase
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.7.7
  
  # AÑADIR ESTO:
  provider: ^6.1.2
  intl: ^0.19.0
  google_mobile_ads: ^7.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"


flutter:
  uses-material-design: true
  
  # 🖼️ ASSETS (Incluimos el directorio completo para el avatar)
  assets:
    - assets/